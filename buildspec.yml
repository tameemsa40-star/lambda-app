version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - zip -r lambda.zip index.js node_modules

artifacts:
  files:
    - lambda.zip